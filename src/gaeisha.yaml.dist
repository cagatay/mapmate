# path the directory where template files reside in.
# path must be written relative to the path of gaeisha

template_path: 'templates/'
controller_package: 'controllers'
debug: yes

# application specific configuration

#### facebook ####
facebook_auth_link : 'https://graph.facebook.com/oauth/authorize?'
facebook_app_secret : ###,
facebook_app_key : ###,
facebook_param : {
    display : touch,
    client_id : ###,
    redirect_uri : 'http://localhost:8080'
}
